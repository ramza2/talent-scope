# Traefik configuration placeholders

Future production routing:

- `/` → frontend
- `/api/*` → api

Traefik Docker Provider + label-based routing will be added when deployment work starts.
Do not introduce a separate API Gateway product.
