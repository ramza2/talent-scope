# TalentScope Frontend

React + TypeScript + Vite + Ant Design + TanStack Query + React Router skeleton.

## Scripts

```bash
npm install
npm run dev
npm run build
```

Dev server proxies `/api` to `http://127.0.0.1:8000`.

Business screens are placeholders only — no Person/Search/Analysis APIs are wired yet.
